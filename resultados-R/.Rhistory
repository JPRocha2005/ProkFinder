source("montando-graficos.R")
ls()
checkm2_quality
checkm2_quality[translation_table_used]
checkm2_quality$translation_table_used
table(checkm2_quality$translation_table_used)
table(checkm2_quality$completeness_model_used)
count(checkm2_quality$completeness_model_used)
source("montando-graficos.R")
ggplot(mtcars, aes(x=as.factor(cyl), y=mpg)) + 
    geom_boxplot(fill="slateblue", alpha=0.2) + 
    xlab("cyl")
mtcars
ggplot(mtcars, aes(x=as.factor(cyl), y=mpg)) + 
    geom_boxplot(fill="slateblue", alpha=0.2) + 
    xlab("cyl")]
ggplot(mtcars, aes(x=as.factor(cyl), y=mpg)) + 
    geom_boxplot(fill="slateblue", alpha=0.2) + 
    xlab("cyl")]
ggplot(mtcars, aes(x=as.factor(cyl), y=mpg)) + 
    geom_boxplot(fill="slateblue", alpha=0.2) + 
    xlab("cyl")
ls

source("montando-graficos.R")
source("montando-graficos.R")
pdf("violin_quality_comparison.pdf", width = 7, height = 5)
walk(plots, print)
dev.off()
source("montando-graficos.R")
source("montando-graficos.R")
source("montando-graficos.R")
source("montando-graficos.R")
ls()
ls(class(tibble))
ls(tibble)
colnames(gtdb_gtdbtk_tax_table)
gtdb_gtdbtk_tax_table
col_names(therm_label_data)
colnames(therm_label_data)
source("montando-graficos.R")
source("montando-graficos.R")
source("montando-graficos.R")
l
ls
q()
